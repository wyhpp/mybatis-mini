<!--<?xml version="1.0" encoding="utf-8"?>-->
<!--<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"-->
<!--        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">-->

<mapper namespace="Dao.IUserDao">

    <select id="queryUserInfoById" parameterType="java.lang.Long" resultType="pojo.User">
        SELECT id, userId, userHead, userName
        FROM user
        where id = #{id} and userId = #{userId1,jdbcType = VARCHAR}
    </select>

    <select id="queryUserInfo" parameterType="pojo.User" resultType="pojo.User">
        SELECT id, userId, userName, userHead
        FROM user
        where id = #{id} and userId = #{userId}
    </select>

</mapper>